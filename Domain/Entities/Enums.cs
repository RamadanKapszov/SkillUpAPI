namespace SkillUpAPI.Domain.Entities
{
    public enum UserRole { Admin, Teacher, Student }
    public enum QuestionType { Single, Multiple, Boolean }
    public enum BadgeCondition { TotalPoints, TestsCompleted, CourseCompleted }
}
